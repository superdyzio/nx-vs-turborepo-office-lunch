{
  "name": "shared-models",
  "projectType": "library",
  "sourceRoot": "libs/shared/models/src",
  "tags": ["scope:shared"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["libs/shared/models/**/*.ts"]
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest --run --config libs/shared/models/vitest.config.ts"
      }
    }
  }
}
