{
  "name": "feature-ordering",
  "projectType": "library",
  "sourceRoot": "libs/feature-ordering/src",
  "tags": ["scope:feature"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": { "lintFilePatterns": ["libs/feature-ordering/**/*.ts"] }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "vitest --run --config libs/feature-ordering/vitest.config.ts", "cwd": "{workspaceRoot}" }
    }
  }
}
