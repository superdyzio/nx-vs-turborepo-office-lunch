{
  "name": "feature-departure",
  "projectType": "library",
  "sourceRoot": "libs/feature-departure/src",
  "tags": ["scope:feature"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": { "lintFilePatterns": ["libs/feature-departure/**/*.ts"] }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "vitest --run --config libs/feature-departure/vitest.config.ts", "cwd": "{workspaceRoot}" }
    }
  }
}
