{
  "name": "feature-admin",
  "projectType": "library",
  "sourceRoot": "libs/feature-admin/src",
  "tags": ["scope:feature"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": { "lintFilePatterns": ["libs/feature-admin/**/*.ts"] }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "vitest --run --config libs/feature-admin/vitest.config.ts", "cwd": "{workspaceRoot}" }
    }
  }
}
